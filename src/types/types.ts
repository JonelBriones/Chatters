export interface UserInterface {
  userId: number;
  user: {
    email: string;
    image: string;
    name: string;
    id: string;
  };
}
export interface SessionUser {
  email: string;
  image: string;
  name: string;
  id: string;
}
